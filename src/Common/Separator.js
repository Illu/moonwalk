import React from "react";
import styled from "styled-components";

const StyledView = styled.View`
  height: 1px;
  background: red;
  margin: 0px 35px;
`;

const Separator = () => <StyledView />;

export default Separator;
